apiVersion: v1
kind: Pod
metadata:
  name: pod
  namespace: dev
spec:
  containers:
  - name: cont
    image: nginx
    ports:
    - containerPort: 80
    resources:
      requests:
        cpu: "50m"
        memory: "50Mi"
      limits:
        cpu: "200m"
        memory: "200Mi"
